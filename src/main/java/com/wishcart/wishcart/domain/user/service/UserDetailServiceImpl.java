package com.wishcart.wishcart.domain.user.service;

import org.springframework.stereotype.Service;

@Service
public class UserDetailServiceImpl // implements UserDetailsService
{
	/*
	 * @Autowired private UserRepository userRepository;
	 */

	/*
	 * @Override public UserDetails loadUserByUsername(String username) throws UsernameNotFoundException { UserEntity
	 * userEntity = userRepository.findByUserName(username); if(userEntity == null){ throw new
	 * UsernameNotFoundException("User not found with userName:"+username); } }
	 */
}
